# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterFileHashBindings
description: |
  Bindings for `src/flutter_file_hash.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_file_hash_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_file_hash.h'
  include-directives:
    - 'src/flutter_file_hash.h'
ffi-native:
comments:
  style: any
  length: full
